create_table("users") {
    t.Column("id", "uuid", {primary: true})
    t.Column("email", "string", {})
    t.Column("password", "string", {})
    t.Timestamps()
    t.Index("email", {"unique": true})
}

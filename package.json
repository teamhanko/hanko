{
  "name": "hanko-monorepo",
  "version": "1.0.0",
  "description": "hanko-monorepo",
  "private": true,
  "workspaces": [
    "frontend/frontend-sdk",
    "frontend/elements",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "dependencies": {
    "turbo": "^1.8.2"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
